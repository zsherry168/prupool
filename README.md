# PruPool Carpool Application

Description

# Running the flask page

## Windows

```bash
pip install -r requirements.txt
set FLASK_APP=run.py
flask run
```

## Mac

```zsh
pip install -r requirements.txt (if not working try pip3)
export FLASK_APP=run.py
flask run
```
